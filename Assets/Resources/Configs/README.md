# ScriptableObject Configurations

Create ScriptableObject assets here to tweak gameplay, economy and matchmaking
parameters without editing code. Suggested assets:

- `PongGameConfig` – Ball speed, scoring target and rewards for practice mode.
- `ServiceConfig` – Assign specific service implementations (mock vs production).
- Progression tables (arenas, XP thresholds, reward tracks).
